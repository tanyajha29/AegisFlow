import { useParams } from 'react-router-dom';

function UserProfile() {
  const { id } = useParams(); // ID is taken directly from the URL

  useEffect(() => {
    // Attacker can manually change the URL ID to view other users' data
    fetch(`/api/users/${id}`)
      .then(res => res.json())
      .then(data => setUser(data));
  }, [id]);

  return <div>{user.name}'s Profile</div>;
}
